<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_editar_password"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:weightSum="1"
    tools:context="com.example.semauleo.globalparkmeters.EditarPassword">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:id="@+id/editPassword"
        android:contentDescription="Contraseña"
        android:hint="Contraseña" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:layout_below="@+id/editPassword"
        android:layout_alignEnd="@+id/editPassword"
        android:layout_marginTop="24dp"
        android:id="@+id/editRePassword"
        android:hint="Repiete contraseña" />

    <Button
        android:text="Cambiar contraseña"
        android:layout_height="wrap_content"
        android:id="@+id/btnEditPass"
        android:background="@android:color/holo_blue_light"
        android:textStyle="bold"
        android:layout_marginTop="30dp"
        android:layout_below="@+id/editRePassword"
        android:layout_centerHorizontal="true"
        android:layout_width="200dp" />

</RelativeLayout>
